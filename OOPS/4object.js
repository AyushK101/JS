//? In js , ARRAY is a custom object , where `key=>index` and the value is the given value to the corresponding index.

let arr = [2, 3, 4, 2];

let obj = {
  x: 23,
  y: 34,
};

// freeze any updation in obj 
Object.freeze(obj);
