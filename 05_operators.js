// arithmetic operators 
// + , - , / , % , *
console.log(10/2); //will give value in decimal in js unlike c++ 
// if we want have floor divisions 
// 1 way => parseint(10/2);
// 1 way => Math.floor(5/2);


// assignment operators 
 let x = 14; // assigning the value 14 to x 
// -= , /= , %= , *= , +=


// logical operators 
// : the operands are evaluated in boolean first and then operators are applied. 
// AND &&
// OR ||
// NOT !

// interesting cases
console.log(4 && 0); //0
console.log(4 || 0); //4
console.log(0 && 4); //0
console.log(0 || 4); //4
// concept of short circuit like in physics's logic gate.

// if first value in the AND gate is true , then you return second value.
// if first value in the AND gate is false , then you return first value.
// if first value is false in OR gate , then second value gets return.
// if first value is true in OR  gate , you return the first value.

// concept like mathematic reasoning**
// in OR gate , if first is false , then it depends upon 2nd.
// in AND gate, if first is true , then it depends upon 2nd.
